{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "lldb",
      "name": "Debug",
      "request": "launch",
      "program": "${fileDirname}/../target/debug/${workspaceRootFolderName}",
      "args": [],
      "cwd": "${workspaceRoot}",
      "sourceLanguages": [
        "rust"
      ]
    }
  ]
}
